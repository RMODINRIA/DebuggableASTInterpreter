SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'DebuggableASTInterpreter',
      #directory : '.',
      #load : 'default',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
      #ignoreImage : true
    }
  ],
  #testing : { 
  	#packages: [ 'Debuggable.*' ]
  }
}